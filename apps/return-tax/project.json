{
  "name": "return-tax",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/return-tax",
  "projectType": "application",
  "tags": [],
  "targets": {
    "build": {
      "executor": "@nx/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/apps/return-tax"
      },
      "configurations": {
        "development": {
          "outputPath": "apps/return-tax"
        },
        "production": {}
      }
    },
    "serve": {
      "executor": "@nx/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "return-tax:build",
        "dev": true
      },
      "configurations": {
        "development": {
          "buildTarget": "return-tax:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "return-tax:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nx/next:export",
      "options": {
        "buildTarget": "return-tax:build:production"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/return-tax/jest.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
